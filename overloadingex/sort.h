#include<iostream>
#include <string>
#include<string.h>

using namespace std;

class Sort {
public:
	int* sort(int* myarr, int len);
	double* sort(double* myarr, int len);
	string* sort(string** myarr, int len);
};